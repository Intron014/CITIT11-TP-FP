#include <stdio.h>
#include <string.h>

void encriptar(char *nombre){
	int i;
	printf("Introduzca le Name and Surnames: ");
	gets(nombre);
	for(i=0;nombre[i];i++){
		nombre[i]=toupper(nombre[i]);
	if(strchr(vocales, nombre[i])
		nombres[i]+=3
	else
		nombre[i]+=7
	}
}
